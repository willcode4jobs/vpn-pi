fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# IDS blind-relay envelope (nodes sign+seal, master opens+verifies). Not needed
# on the hub. aarch64 + x86_64 wheels exist; on sirius use a py3.12 venv.
pynacl==1.5.0
